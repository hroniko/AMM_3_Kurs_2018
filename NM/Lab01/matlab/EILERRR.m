% На всякий пожарный случай, не для вычисления

[X Y]=ode45('F3',[0,1],[1,1]);

plot(X,Y(:,1)); hold on;
plot(X,Y(:,2));
plot(X1,Z1,'ro');
plot(X1,Y1,'bo');

a=0;b=1;N=10;
h = abs(b-a)/N; % Расстояние между узлами
X = a:h:b; % Формируем вектор X узловых точек

[X Y]=ode45('F3',X,[1,1]);

[X Y]=ode15s('F3',X,[1,1]);
